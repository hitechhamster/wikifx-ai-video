# ============================================================
# 绝不进仓库的东西
# ============================================================

# —— 密钥!真实配置(含 API key),只留 config.example.toml ——
config.toml

# —— 运行时产物 / 缓存(几个 G,同事跑时自动重新生成)——
storage/
output/
resource/songs/_tpl_*.mp3

# —— Python ——
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# —— 编辑器 / 系统 ——
.idea/
.vscode/
.DS_Store
Thumbs.db

# —— 临时 ——
*.log
scratchpad/
